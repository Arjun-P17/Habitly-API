server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/habitly
    username: habitly
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect